apiVersion: 1

providers:
  - name: security-lab
    orgId: 1
    folder: Linux Security Lab
    folderUid: linux-security-lab
    type: file
    updateIntervalSeconds: 30
    allowUiUpdates: false
    disableDeletion: false

    options:
      path: /etc/grafana/dashboards
